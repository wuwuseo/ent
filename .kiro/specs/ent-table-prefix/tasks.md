# 实现计划

- [x] 1. 实现 TablePrefixExtension 核心结构





  - 在 `ent.go` 中为 `TablePrefixExtension` 添加 `prefix` 字段用于存储表前缀配置
  - 确保结构体正确继承 `entc.DefaultExtension`
  - _需求: 1.1, 1.3, 3.1_
-

- [x] 2. 实现 WithTablePrefix 配置函数



  - 实现 `WithTablePrefix(prefix string)` 函数，接受前缀参数并返回配置好的扩展实例
  - 在函数中创建并初始化 `TablePrefixExtension` 实例，将前缀存储到结构体字段
  - 添加基本的前缀验证逻辑（检查是否为合法的 SQL 标识符）
  - 支持空前缀的情况（空前缀时不修改表名）
  - _需求: 2.1, 2.2, 2.3, 2.4_

- [x] 3. 更新 Templates 方法实现
  - 修改 `Templates()` 方法，确保能够正确加载和解析 `tableprefix.tmpl` 模板
  - 将扩展实例（包含前缀配置）注入到模板上下文中，使模板可以访问前缀
  - 使用 `gen.MustParse` 确保模板解析失败时立即报错
  - 使用 Go embed 特性嵌入模板文件，避免路径问题
  - 添加错误处理和日志记录
  - _需求: 1.3, 3.2, 3.3, 4.2_

- [x] 4. 完善 tableprefix.tmpl 模板




  - 修复模板文件的语法错误（当前有 EOF 错误）
  - 实现完整的模板逻辑：遍历所有实体节点 `$.Nodes`
  - 为每个实体生成 `Table()` 方法，返回带前缀的表名
  - 在模板中访问扩展的前缀配置 `$.Extension.Prefix`
  - 添加条件判断：如果前缀为空则返回原始表名
  - 生成正确的包导入语句
  - 添加模板注释说明生成逻辑
  - _需求: 4.1, 4.2, 4.3, 4.4, 1.4_

- [x] 5. 创建使用示例文件





  - 创建 `examples/generate.go` 文件展示如何在 Ent 项目中使用此扩展
  - 示例包含基本用法：调用 `WithTablePrefix` 并注册到 `entc.Generate`
  - 添加多环境配置示例（根据环境变量使用不同前缀）
  - 添加与其他扩展组合使用的示例
  - 在示例中添加详细的中文注释
  - _需求: 5.1, 5.2, 5.3_

- [ ]* 6. 编写单元测试
  - 创建 `ent_test.go` 文件
  - 测试 `WithTablePrefix` 函数：验证扩展实例创建和前缀存储
  - 测试 `Templates()` 方法：验证返回的模板列表非空且可解析
  - 测试空前缀处理：验证空字符串前缀不会导致错误
  - 测试特殊字符前缀：验证非法字符的处理
  - 测试前缀验证逻辑的各种边界情况
  - _需求: 2.1, 2.2, 2.3, 2.4, 3.2_

- [ ]* 7. 编写集成测试
  - 创建 `integration_test.go` 文件
  - 创建测试用的 Schema 定义（如 User、Post 实体）
  - 在临时目录中执行完整的代码生成流程
  - 验证生成的代码文件存在且包含正确的表名前缀
  - 验证生成的代码可以成功编译
  - 测试多个实体的前缀应用
  - 测试与其他扩展的兼容性
  - _需求: 1.1, 1.2, 1.3, 1.4, 3.3, 4.3, 5.1_




- [ ] 8. 创建 README 文档

  - 创建 `README.md` 文件，包含项目简介和功能说明
  - 添加安装说明（go get 命令）
  - 添加快速开始指南，展示基本用法
  - 添加完整的 API 文档（WithTablePrefix 函数说明）
  - 添加使用示例代码片段
  - 添加常见问题解答（FAQ）
  - 添加贡献指南和许可证信息
  - 所有文档使用中文编写
  - _需求: 5.1, 5.2, 5.3, 5.4_

- [ ] 9. 添加前缀验证和错误处理
  - 在 `WithTablePrefix` 中实现前缀验证函数
  - 验证前缀是否为合法的 SQL 标识符（字母、数字、下划线，不以数字开头）
  - 添加前缀长度限制检查（建议不超过 20 字符）
  - 对于非法前缀，返回有意义的错误信息
  - 在模板中添加错误处理逻辑
  - 添加日志记录以便调试
  - _需求: 2.4, 4.4_

- [ ]* 10. 性能优化和最终验证
  - 验证模板解析只在启动时执行一次
  - 确认生成的 `Table()` 方法返回常量字符串（无运行时计算）
  - 使用 benchmark 测试代码生成性能（针对大量实体）
  - 验证生成的代码不影响 Ent 查询性能
  - 检查内存使用情况
  - 进行代码审查，确保符合 Go 最佳实践
  - _需求: 3.3, 3.4_
